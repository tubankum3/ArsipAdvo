// ====== CONFIGURATION ======
// After deploying apps-script/Code.gs as a Web App (see README.md),
// paste the Web App URL below. It looks like:
// https://script.google.com/macros/s/XXXXXXXXXXXXXXXXXXXX/exec
const CONFIG = {
  API_URL: 'PASTE_YOUR_APPS_SCRIPT_WEB_APP_URL_HERE',
  // Optional — only needed if you set an API_TOKEN Script Property in
  // Code.gs (see the comment above getApiToken_ in that file). Leave as
  // '' if you didn't set one.
  API_TOKEN: ''
};
